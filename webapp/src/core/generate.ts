import { type TreeNode } from '.';

/**
 * 根节点表
 * ```js
  { root1: [TreeNode1, TreeNode2, ...], root2: [...], ... }
 * ```
 */
const ROOT_TABLE = new Map<string, TreeNode[]>();

/**
 * 生成HTML节点树
 */
export function generate() {}
